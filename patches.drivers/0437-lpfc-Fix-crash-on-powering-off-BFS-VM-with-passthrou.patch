From: James Smart <jsmart2021@gmail.com>
Date: Tue, 30 May 2017 22:41:08 -0700
Subject: lpfc: Fix crash on powering off BFS VM with passthrough device
References: bsc#1041873
Patch-Mainline: submitted linux-scsi 2017/06/01

Null pointer dereference when BFS VM is powered off

The driver incorrectly uses sli3_ring on SLI-4 adapters

Use the correct ring structure based on sli_rev

Signed-off-by: Dick Kennedy <dick.kennedy@broadcom.com>
Signed-off-by: James Smart <james.smart@broadcom.com>
Acked-by: Hannes Reinecke <hare@suse.com>
---
 drivers/scsi/lpfc/lpfc_sli.c | 14 +++++++++++---
 1 file changed, 11 insertions(+), 3 deletions(-)

diff --git a/drivers/scsi/lpfc/lpfc_sli.c b/drivers/scsi/lpfc/lpfc_sli.c
index 1310ab3..1ef1137 100644
--- a/drivers/scsi/lpfc/lpfc_sli.c
+++ b/drivers/scsi/lpfc/lpfc_sli.c
@@ -10951,6 +10951,7 @@ lpfc_sli_abort_iocb(struct lpfc_vport *vport, struct lpfc_sli_ring *pring,
 	struct lpfc_hba *phba = vport->phba;
 	struct lpfc_iocbq *iocbq;
 	struct lpfc_iocbq *abtsiocb;
+	struct lpfc_sli_ring *pring_s4;
 	IOCB_t *cmd = NULL;
 	int errcnt = 0, ret_val = 0;
 	int i;
@@ -11005,12 +11006,19 @@ lpfc_sli_abort_iocb(struct lpfc_vport *vport, struct lpfc_sli_ring *pring,
 		/* Setup callback routine and issue the command. */
 		abtsiocb->iocb_cmpl = lpfc_sli_abort_fcp_cmpl;
 
-		/* In PCI hotplug remove path, pring might be NULL */
-		if (pring)
+		ret_val = IOCB_ERROR;
+		if (phba->sli_rev == LPFC_SLI_REV4) {
+			pring_s4 = lpfc_sli4_calc_ring(phba, iocbq);
+			if (pring_s4)
+				ret_val = lpfc_sli_issue_iocb(phba,
+							      pring_s4->ringno,
+							      abtsiocb, 0);
+		} else if (pring)
+			/* In PCI hotplug remove path, pring might be NULL */
 			ret_val = lpfc_sli_issue_iocb(phba, pring->ringno,
 						      abtsiocb, 0);
 
-		if (!pring || ret_val == IOCB_ERROR) {
+		if (ret_val == IOCB_ERROR) {
 			lpfc_sli_release_iocbq(phba, abtsiocb);
 			errcnt++;
 			continue;
-- 
1.8.5.6

